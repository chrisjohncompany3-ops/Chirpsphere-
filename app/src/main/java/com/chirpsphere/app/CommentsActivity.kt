package com.chirpsphere.app

import android.os.Bundle
import android.widget.Toast
import androidx.appcompat.app.AppCompatActivity
import com.chirpsphere.app.models.Post

class CommentsActivity : AppCompatActivity() {
        override fun onCreate(savedInstanceState: Bundle?) {
                    super.onCreate(savedInstanceState)
                            setContentView(R.layout.activity_main) // Replace later with activity_comments.xml

                                    // Placeholder
                                            Toast.makeText(this, "Comments section coming soon!", Toast.LENGTH_SHORT).show()
        }
}
        }
}